<!DOCTYPE html>
<html>
  <head>
    <title>VaultPoc</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

    <%= yield %>

    <script>
      window.VAULT_ENV = {
        BITWARDEN_PASSWORDLESS_API_PUBLIC_KEY: '<%= ENV['BITWARDEN_PASSWORDLESS_API_PUBLIC_KEY'] %>'
      }
    </script>
  </body>
</html>
